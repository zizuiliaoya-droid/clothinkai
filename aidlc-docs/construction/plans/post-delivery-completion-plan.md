# 交付后遗留功能收口计划

> 用户已批准按全仓审计建议直接实施；中文、现有技术栈和上线流程不重新审批。

## 1. 数据安全与契约
- [x] 1.1 SKU 引用检查接入推广与拍单/刷单真实计数。
- [x] 1.2 博主引用检查接入推广真实计数。
- [x] 1.3 Worker 结果状态严格校验，Worker Token 增加安全列表接口。

## 2. 自动采集 Worker
- [x] 2.1 建立可运行的 Python Worker、配置、HTTP 客户端和轮询编排。
- [x] 2.2 提供千牛、万相台、灰豚命令适配器与安全临时文件处理。
- [x] 2.3 补齐部署说明、示例环境变量和容器入口。

## 3. 采集运营前端
- [x] 3.1 增加平台凭据管理页面。
- [x] 3.2 增加 Worker Token 签发/列表/吊销页面。
- [x] 3.3 增加数据质量汇总、筛选和处理页面。

## 4. 报表闭环
- [x] 4.1 投产趋势增加退款、确认金额、站外费、总花费和 ROI。
- [x] 4.2 工作进度、店铺数据、投产报表增加 Excel 导出。
- [x] 4.3 暴露 Content-Disposition 并保持日期筛选口径。

## 5. 范围与基础设施收口
- [x] 5.1 下线已取消的公开套装 API，保留历史表与迁移数据。
- [x] 5.2 配置备份导出角色、权限、字段映射、字典和用户偏好白名单。
- [x] 5.3 清理首页过期文案、死代码和过期 TODO。

## 6. 验证与发布
- [x] 6.1 diagnostics、前端 build、Python compileall、现有测试、migration null-byte/AST、diff check。（pytest 已执行但被本机缺少既有依赖 `boto3` 在 conftest 加载阶段阻断；未擅自安装依赖，其余检查与独立 smoke 均通过。）
- [x] 6.2 提交并推送 main，等待 Zeabur 前后端部署 RUNNING，检查 `/ready`。
- [x] 6.3 真实登录线上验收新增页面、报表、权限和已下线路由；清理测试数据。
  - 管理员通过真实登录表单进入 `Initial Admin` 会话，未手工写入或伪造 Token；平台凭据完整生命周期、Worker Token 安全契约与吊销、数据质量入口、三类报表/导出、四种投产趋势粒度、套装路由 404、响应式和控制台均已验证。
  - 临时平台凭据已永久删除；临时 Worker Token 已吊销并保留不可用审计记录；本地报表下载文件未提交。
  - 142 条现有生产数据质量警告未执行“标记已修复”或“忽略”，避免不可逆修改真实业务问题。
  - operations-only/warehouse 角色因无可安全使用的专用凭据未执行真实登录；未猜测、重置或暴力尝试生产账号，权限与路由限制仅完成代码核对。
  - SKU/博主引用删除保护未对生产数据执行 DELETE；无只读引用检查端点，破坏性尝试可能删除真实数据，故仅保留代码级和本地验证结论。
  - 外部 Worker 无生产 service ID 和真实合规平台命令，未生产部署且不声明生产部署完成。
- [x] 6.4 更新 aidlc-state.md，audit.md 仅追加并提交收尾记录。
