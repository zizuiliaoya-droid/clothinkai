# =============================================================================
# Clothing ERP Backend — Development & Test Dependencies
# =============================================================================

-r requirements.txt

# 测试
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-mock==3.14.0
pytest-postgresql==6.1.1
factory-boy==3.3.1
faker==30.1.0
freezegun==1.5.1

# Lint / 类型 / 格式化
ruff==0.6.9
mypy==1.11.2
# 不要加 types-redis：redis-py 5.x 自带 py.typed 内联类型，装了 stub 反而会被优先
# 采用，而 types-redis 4.6 的接口落后于 pinned 的 redis 5.1（例如缺 aclose），
# 会产出假错误。
types-passlib==1.7.7.20240819
types-python-jose==3.3.4.20240106
